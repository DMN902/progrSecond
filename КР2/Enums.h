#pragma once

#define TestSize 4
#define ControlSize 5


enum MainMenu {file = 1, Manual};

enum SecondMenu {showList = 1, addTransport, deleteTransport, saveList, back, closeProgram};

enum TypeOfTransport { carType = 1, sportsCarType, truckType, transportType };